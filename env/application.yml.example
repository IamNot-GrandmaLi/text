# 环境配置参考文件（入库）
# 使用方法：复制本文件为 env/application.yml，填入真实配置后使用
# env/application.yml 已被 gitignore，不会提交到仓库
alarm:
  kafka:
    bootstrap-servers: <KAFKA地址:端口>
    topic: alarm_message
    # SASL 认证，默认关闭；开启时填写 jaas 配置
    sasl-enabled: false
    sasl-jaas-config:
